doctype 5
html(lang="en")
  head
    link(href="//api.tiles.mapbox.com/mapbox.js/v1.3.1/mapbox.css" rel="stylesheet")
    link(href="/mapbox.js/assets/leaflet.draw.css")

    script(src="//api.tiles.mapbox.com/mapbox.js/v1.3.1/mapbox.js")
    script(src="//ajax.googleapis.com/ajax/libs/jquery/1.10.2/jquery.min.js")
    script(src="/mapbox.js/assets/leaflet.draw.js")

    :coffeescript
      $ ->
        map = L.mapbox.map("map", "willrax.map-vaendp9j").setView([-33.914, 151.2328], 13)
        featureGroup = L.featureGroup().addTo(map);

    style
      body{
        margin: 0;
        padding: 0;
      }
      #map{
        position: absolute;
        top: 0;
        bottom: 0;
        width: 100%;
      }

    title Map

  body
    #map
